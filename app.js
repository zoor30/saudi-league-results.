import * as THREE from 'https://unpkg.com/three@0.169.0/build/three.module.js';
import {GLTFLoader} from 'https://unpkg.com/three@0.169.0/examples/jsm/loaders/GLTFLoader.js';
import * as SkeletonUtils from 'https://unpkg.com/three@0.169.0/examples/jsm/utils/SkeletonUtils.js';

const SHEEP_URL='https://raw.githubusercontent.com/lord3nd3r/ffxi-browser/main/public/models/sheep.glb';
const TRUCK_URL='https://raw.githubusercontent.com/KenneyNL/Starter-Kit-Racing/main/models/vehicle-truck-yellow.glb';
const $=s=>document.querySelector(s),clamp=(v,a,b)=>Math.max(a,Math.min(b,v)),rand=(a,b)=>a+Math.random()*(b-a);

const scene=new THREE.Scene();scene.background=new THREE.Color(0x95d4eb);scene.fog=new THREE.FogExp2(0xc8d7ce,.013);
const camera=new THREE.PerspectiveCamera(36,innerWidth/innerHeight,.1,140);camera.position.set(13,18,22);
const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});renderer.setSize(innerWidth,innerHeight);renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.08;$('#game').appendChild(renderer.domElement);
scene.add(new THREE.HemisphereLight(0xffefd1,0x75583c,2.35));const sun=new THREE.DirectionalLight(0xffdca8,4.15);sun.position.set(-14,24,10);sun.castShadow=true;sun.shadow.mapSize.set(2048,2048);Object.assign(sun.shadow.camera,{left:-30,right:30,top:30,bottom:-30});scene.add(sun);
const mat=(c,r=.8,m=0)=>new THREE.MeshStandardMaterial({color:c,roughness:r,metalness:m});
function mesh(geo,c,p=[0,0,0],r=.8,m=0,parent=scene){const o=new THREE.Mesh(geo,mat(c,r,m));o.position.set(...p);o.castShadow=o.receiveShadow=true;parent.add(o);return o}

const ground=mesh(new THREE.PlaneGeometry(90,90),0xd3ad6b);ground.rotation.x=-Math.PI/2;
for(let i=0;i<110;i++){const d=mesh(new THREE.CircleGeometry(rand(.1,.72),10),Math.random()>.52?0xbc8f50:0xe1bd79,[rand(-27,27),.012,rand(-27,27)]);d.rotation.x=-Math.PI/2;d.scale.y=rand(.28,.58)}
for(let i=0;i<34;i++){const g=mesh(new THREE.ConeGeometry(rand(.035,.07),rand(.18,.34),5),0x8a8b4b,[rand(-10,10),.1,rand(-8.8,8.8)]);g.rotation.z=rand(-.2,.2)}

for(let x=-12;x<=12;x+=1.5){mesh(new THREE.CylinderGeometry(.08,.1,1.55,8),0x72583d,[x,.78,-11]);mesh(new THREE.CylinderGeometry(.08,.1,1.55,8),0x72583d,[x,.78,11])}
for(let z=-9.5;z<=9.5;z+=1.5){mesh(new THREE.CylinderGeometry(.08,.1,1.55,8),0x72583d,[-12,.78,z]);mesh(new THREE.CylinderGeometry(.08,.1,1.55,8),0x72583d,[12,.78,z])}
for(const z of[-11,11])for(const y of[.4,.85,1.28])mesh(new THREE.BoxGeometry(24,.055,.055),0x62605a,[0,y,z],.45,.55);
for(const x of[-12,12])for(const y of[.4,.85,1.28])mesh(new THREE.BoxGeometry(.055,.055,22),0x62605a,[x,y,0],.45,.55);
for(const x of[-8,-1])for(const z of[-7,-2])mesh(new THREE.CylinderGeometry(.09,.11,3.2,8),0x6d5942,[x,1.6,z]);const roof=mesh(new THREE.BoxGeometry(7.7,.12,5.8),0x516a59,[-4.5,3.2,-4.5]);roof.rotation.z=-.025;
const feeder=mesh(new THREE.BoxGeometry(5.7,.5,1.15),0x8a8071,[3.4,.27,-5.2]);const grain=mesh(new THREE.BoxGeometry(5.15,.1,.75),0xc89c3d,[3.4,.56,-5.2]);const waterTrough=mesh(new THREE.BoxGeometry(3.4,.55,1.45),0x777e7d,[5.4,.3,5.4],.45,.6);const water=mesh(new THREE.BoxGeometry(3.05,.05,1.08),0x3e9fc4,[5.4,.59,5.4],.18,.1);water.material.transparent=true;
for(let i=0;i<5;i++){let h=mesh(new THREE.CylinderGeometry(.65,.65,1.1,20),0xd0a12d,[-8+i*1.2,.68,7]);h.rotation.z=Math.PI/2}

const obstacles=[{x:3.4,z:-5.2,hx:3.25,hz:1.25},{x:5.4,z:5.4,hx:2.1,hz:1.45},{x:-8,z:-7,hx:.55,hz:.55},{x:-1,z:-7,hx:.55,hz:.55},{x:-8,z:-2,hx:.55,hz:.55},{x:-1,z:-2,hx:.55,hz:.55}];
const feedSlots=[],waterSlots=[];for(let i=0;i<5;i++)feedSlots.push({p:new THREE.Vector3(1.55+i*.92,0,-3.95),owner:null},{p:new THREE.Vector3(1.55+i*.92,0,-6.45),owner:null});for(let i=0;i<4;i++)waterSlots.push({p:new THREE.Vector3(4.15+i*.76,0,3.85),owner:null},{p:new THREE.Vector3(4.15+i*.76,0,6.95),owner:null});

const loader=new GLTFLoader(),sheep=[],mixers=[],fx=[];let sheepSource=null,sheepAnims=[],truck=null,truckOn=false,feedStock=12,troughFood=35,waterLevel=100,selected=null;
const stateNames={wander:'يتمشى داخل الحوش',idle:'واقف ويتلفّت',seekFood:'شمّ الشعير ورايح للمعلف',eat:'يأكل من الشعير',seekWater:'رايح يشرب',drink:'يشرب من الحوض',rest:'يرتاح',social:'يلحق القطيع',startled:'مفزوع من الوايت',curious:'يتفقد المكان',graze:'ينقّر الأرض'};
const personalities=['هادية','فضولية','اجتماعية','شرهة','نشيطة','حذرة'];
function shadows(root){root.traverse(o=>{if(o.isMesh||o.isSkinnedMesh){o.castShadow=o.receiveShadow=true;o.frustumCulled=false}})}
function normalize(root,h){root.updateMatrixWorld(true);const b=new THREE.Box3().setFromObject(root),n=Math.max(.001,b.max.y-b.min.y);root.scale.multiplyScalar(h/n);root.updateMatrixWorld(true);const b2=new THREE.Box3().setFromObject(root);root.position.y-=b2.min.y}
function blackNajdi(root,ram){root.traverse(o=>{if(o.isMesh||o.isSkinnedMesh){o.material=Array.isArray(o.material)?o.material.map(m=>m.clone()):o.material.clone();for(const m of(Array.isArray(o.material)?o.material:[o.material]))if(m.color){const l=m.color.r+m.color.g+m.color.b;l>1.25?m.color.set(ram?0x17130f:0x24201b):m.color.multiplyScalar(.32);m.roughness=.77}}})}
function findClip(rx,fallback){return sheepAnims.find(a=>rx.test(a.name))||fallback}
function setAnim(s,name){const u=s.userData;if(!u.mixer)return;const clip=name==='walk'?u.clips.walk:name==='eat'?u.clips.eat:name==='rest'?u.clips.rest:u.clips.idle;if(!clip||u.anim===name)return;const next=u.mixer.clipAction(clip);next.reset().fadeIn(.18).play();if(u.action)u.action.fadeOut(.18);u.action=next;u.anim=name}
function releaseSlot(s){const u=s.userData;if(u.slot){u.slot.owner=null;u.slot=null}}
function nearestFree(slots,s){let best=null,bd=1e9;for(const sl of slots)if(!sl.owner||sl.owner===s){const d=s.position.distanceToSquared(sl.p);if(d<bd){bd=d;best=sl}}if(best){best.owner=s;s.userData.slot=best}return best}
function insideObstacle(x,z,pad=.72){return obstacles.some(o=>Math.abs(x-o.x)<o.hx+pad&&Math.abs(z-o.z)<o.hz+pad)}
function validPoint(x,z){return x>-10.15&&x<10.15&&z>-9.15&&z<9.15&&!insideObstacle(x,z)}
function pickWander(s){releaseSlot(s);for(let k=0;k<24;k++){const x=rand(-9,9),z=rand(-8,8);if(validPoint(x,z)){s.userData.target.set(x,0,z);break}}s.userData.state='wander';s.userData.timer=rand(1.8,6)}
function chooseMate(s){let best=null,bd=1e9;for(const q of sheep)if(q!==s){const d=s.position.distanceToSquared(q.position);if(d<bd){bd=d;best=q}}return best}
function decide(s){const u=s.userData;if(u.startle>0)return;if(u.hunger>58-u.appetite*8&&troughFood>1){const sl=nearestFree(feedSlots,s);if(sl){u.state='seekFood';u.target.copy(sl.p);return}}if(u.thirst>58&&waterLevel>2){const sl=nearestFree(waterSlots,s);if(sl){u.state='seekWater';u.target.copy(sl.p);return}}if(u.energy<20){releaseSlot(s);u.state='rest';u.timer=rand(5,10);return}if(u.social<28+u.socialNeed*12){const mate=chooseMate(s);if(mate){releaseSlot(s);u.target.copy(mate.position).add(new THREE.Vector3(rand(-.9,.9),0,rand(-.9,.9)));u.state='social';u.timer=4;return}}const r=Math.random();if(r<.18+u.curiosity*.12){releaseSlot(s);u.state='curious';u.timer=rand(1.2,3.2)}else if(r<.34){releaseSlot(s);u.state='idle';u.timer=rand(1.2,4)}else if(r<.43){releaseSlot(s);u.state='graze';u.timer=rand(2.5,5)}else pickWander(s)}
function addNajdiTraits(rig,ram){const g=new THREE.Group();rig.add(g);const earMat=mat(0x17130f,.9);for(const z of[-.28,.28]){const ear=new THREE.Mesh(new THREE.CapsuleGeometry(.05,.36,4,8),earMat);ear.position.set(-.4,1.15,z);ear.rotation.x=Math.PI/2;ear.rotation.z=.35;g.add(ear)}if(ram){for(const z of[-.23,.23]){const horn=new THREE.Mesh(new THREE.TorusGeometry(.19,.047,7,16,Math.PI*1.55),mat(0xc3a06b,.8));horn.position.set(-.35,1.25,z);horn.rotation.x=Math.PI/2;g.add(horn)}}rig.userData.traits=g}
function spawnSheep(i){const ram=i===9,rig=SkeletonUtils.clone(sheepSource);blackNajdi(rig,ram);shadows(rig);normalize(rig,ram?1.48:1.21);rig.scale.multiplyScalar(rand(.91,1.04));do rig.position.set(rand(-8,8),0,rand(-7,7));while(!validPoint(rig.position.x,rig.position.z));rig.rotation.y=rand(0,Math.PI*2);addNajdiTraits(rig,ram);const mx=new THREE.AnimationMixer(rig),base=sheepAnims[0],clips={walk:findClip(/walk|run/i,base),idle:findClip(/idle|stand|pose/i,base),eat:findClip(/eat|graze|feed|interact/i,findClip(/idle|stand/i,base)),rest:findClip(/sleep|lay|rest/i,findClip(/idle|stand/i,base))};const pers=personalities[i%personalities.length];rig.userData={index:i,name:ram?'الفحل صنف':'نجدية '+(i+1),personality:pers,target:new THREE.Vector3(),velocity:new THREE.Vector3(),state:'idle',timer:rand(0,3),speed:rand(.56,.82),baseSpeed:rand(.56,.82),ram,hunger:rand(20,54),thirst:rand(18,52),energy:rand(55,96),social:rand(45,92),mixer:mx,clips,action:null,anim:'',slot:null,startle:0,phase:rand(0,10),lastBleat:rand(5,25),curiosity:rand(.15,.95),appetite:rand(.1,.95),socialNeed:rand(.1,.95),look:rand(-1,1)};rig.userData.baseSpeed=rig.userData.speed;scene.add(rig);sheep.push(rig);mixers.push(mx);setAnim(rig,'idle');$('#count').textContent=sheep.length}

function separation(s){const out=new THREE.Vector3();for(const q of sheep){if(q===s)continue;const d=s.position.distanceTo(q.position);if(d>0&&d<1.45)out.add(s.position.clone().sub(q.position).normalize().multiplyScalar((1.45-d)/1.45))}return out}
function cohesion(s){const near=[];for(const q of sheep)if(q!==s&&s.position.distanceTo(q.position)<4.5)near.push(q);if(!near.length)return new THREE.Vector3();const c=new THREE.Vector3();near.forEach(q=>c.add(q.position));c.multiplyScalar(1/near.length);return c.sub(s.position).setY(0).normalize()}
function avoidObstacles(s){const out=new THREE.Vector3();for(const o of obstacles){const cx=clamp(s.position.x,o.x-o.hx,o.x+o.hx),cz=clamp(s.position.z,o.z-o.hz,o.z+o.hz),dx=s.position.x-cx,dz=s.position.z-cz,d=Math.hypot(dx,dz);if(d<1.55){const v=new THREE.Vector3(dx,0,dz);if(v.lengthSq()<.001)v.set(rand(-1,1),0,rand(-1,1));out.add(v.normalize().multiplyScalar((1.55-d)*2.8))}}return out}
function keepInYard(s){const v=new THREE.Vector3();if(s.position.x<-9.65)v.x+=1;if(s.position.x>9.65)v.x-=1;if(s.position.z<-8.65)v.z+=1;if(s.position.z>8.65)v.z-=1;return v.multiplyScalar(2.8)}
function resolveHardCollision(s){for(const o of obstacles){const px=o.hx+.72, pz=o.hz+.72;if(Math.abs(s.position.x-o.x)<px&&Math.abs(s.position.z-o.z)<pz){const dx=s.position.x-o.x,dz=s.position.z-o.z,ox=px-Math.abs(dx),oz=pz-Math.abs(dz);if(ox<oz)s.position.x=o.x+Math.sign(dx||1)*px;else s.position.z=o.z+Math.sign(dz||1)*pz;uTurn(s)}}s.position.x=clamp(s.position.x,-10.25,10.25);s.position.z=clamp(s.position.z,-9.2,9.2)}
function uTurn(s){s.userData.velocity.multiplyScalar(-.25);if(['wander','social','startled'].includes(s.userData.state))pickWander(s)}
function moveAnimal(s,dt){const u=s.userData,to=u.target.clone().sub(s.position).setY(0),dist=to.length();if(dist<.2)return true;const desired=to.normalize();const steer=desired.multiplyScalar(1.35).add(separation(s).multiplyScalar(1.65)).add(avoidObstacles(s)).add(keepInYard(s));if(u.state==='wander'||u.state==='social')steer.add(cohesion(s).multiplyScalar(.22));if(steer.lengthSq()>.0001)steer.normalize();u.velocity.lerp(steer.multiplyScalar(u.speed),.13);const slow=clamp(dist/1.15,.23,1);s.position.addScaledVector(u.velocity,dt*slow);resolveHardCollision(s);if(u.velocity.lengthSq()>.002){const ang=Math.atan2(u.velocity.x,u.velocity.z)-Math.PI/2;s.rotation.y=THREE.MathUtils.lerp(s.rotation.y,ang,.1)}setAnim(s,'walk');return false}

function proceduralPose(s,t){const u=s.userData,g=u.traits;if(!g)return;g.rotation.x=0;g.rotation.z=0;g.rotation.y=0;const breathe=Math.sin(t*1.8+u.phase)*.012;s.scale.y+=(breathe-(s.scale.y-u.baseScaleY||0))*.03;if(u.state==='eat'||u.state==='drink'||u.state==='graze'){g.rotation.z=-.28+Math.sin(t*3.4+u.phase)*.07}else if(u.state==='rest'){g.rotation.z=-.42;g.rotation.x=.06*Math.sin(t+u.phase)}else if(u.state==='curious'||u.state==='idle'){g.rotation.y=.32*Math.sin(t*1.4+u.phase);g.children.forEach((ear,i)=>{if(i<2)ear.rotation.z=.35+Math.sin(t*2.4+u.phase+i)*.12})}}

let audioCtx=null;function ensureAudio(){if(!audioCtx)audioCtx=new(window.AudioContext||window.webkitAudioContext)()}
function bleat(vol=.03){try{ensureAudio();const o=audioCtx.createOscillator(),g=audioCtx.createGain(),f=audioCtx.createBiquadFilter();o.type='sawtooth';o.frequency.setValueAtTime(rand(170,230),audioCtx.currentTime);o.frequency.exponentialRampToValueAtTime(rand(105,150),audioCtx.currentTime+.38);f.type='lowpass';f.frequency.value=650;g.gain.setValueAtTime(0,audioCtx.currentTime);g.gain.linearRampToValueAtTime(vol,audioCtx.currentTime+.025);g.gain.exponentialRampToValueAtTime(.0001,audioCtx.currentTime+.46);o.connect(f);f.connect(g);g.connect(audioCtx.destination);o.start();o.stop(audioCtx.currentTime+.47)}catch{}}
function burst(pos,type='dust',count=4){for(let i=0;i<count;i++){const c=type==='heart'?0xe97278:0xc7a06b,geo=type==='heart'?new THREE.SphereGeometry(.055,6,5):new THREE.SphereGeometry(.06,6,5),p=mesh(geo,c,[pos.x+rand(-.15,.15),.25+rand(0,.2),pos.z+rand(-.15,.15)],1);p.material.transparent=true;fx.push({m:p,t:type==='heart'?1.1:.7,v:new THREE.Vector3(rand(-.12,.12),rand(.18,.34),rand(-.12,.12)),type})}}

function updateNeeds(s,dt){const u=s.userData;u.hunger=clamp(u.hunger+dt*(.58+u.appetite*.35),0,100);u.thirst=clamp(u.thirst+dt*.82,0,100);u.social=clamp(u.social-dt*(.08+u.socialNeed*.08),0,100);if(['wander','seekFood','seekWater','social','startled'].includes(u.state))u.energy=clamp(u.energy-dt*.2,0,100);else if(u.state==='rest')u.energy=clamp(u.energy+dt*3.8,0,100)}
function handleState(s,dt,t){const u=s.userData;updateNeeds(s,dt);u.lastBleat-=dt;if(u.lastBleat<0){if(Math.random()<.42)bleat(.015);u.lastBleat=rand(12,30)}if(truck&&truckOn){const d=s.position.distanceTo(truck.position);if(d<4.5&&u.startle<=0){releaseSlot(s);u.startle=rand(2,3.8);u.state='startled';const away=s.position.clone().sub(truck.position).setY(0);if(away.lengthSq()<.01)away.set(1,0,0);u.target.copy(s.position).add(away.normalize().multiplyScalar(4.2));u.speed=1.2;bleat(.03)}}if(u.startle>0){u.startle-=dt;if(moveAnimal(s,dt)){u.startle=0;u.speed=u.baseSpeed;decide(s)}return}
 if(['wander','social','seekFood','seekWater'].includes(u.state)){const arrived=moveAnimal(s,dt);if(!arrived)return;if(u.state==='seekFood'){u.state='eat';u.timer=rand(4.5,7);setAnim(s,'eat');return}if(u.state==='seekWater'){u.state='drink';u.timer=rand(4,6);setAnim(s,'eat');return}if(u.state==='social'){u.social=clamp(u.social+20,0,100);u.state='idle';u.timer=rand(1,3);setAnim(s,'idle');return}u.timer-=dt;if(u.timer<=0)decide(s);return}
 if(u.state==='eat'){setAnim(s,'eat');u.timer-=dt;if(troughFood>0){u.hunger=clamp(u.hunger-dt*9.2,0,100);troughFood=clamp(troughFood-dt*.78,0,100)}if(Math.random()<dt*.8)burst(s.position,'dust',1);if(u.timer<=0||u.hunger<15||troughFood<=.4){releaseSlot(s);u.state='idle';u.timer=rand(1,3);setAnim(s,'idle')}return}
 if(u.state==='drink'){setAnim(s,'eat');u.timer-=dt;if(waterLevel>0){u.thirst=clamp(u.thirst-dt*10.5,0,100);waterLevel=clamp(waterLevel-dt*.6,0,100)}if(u.timer<=0||u.thirst<14||waterLevel<=.4){releaseSlot(s);u.state='idle';u.timer=rand(1,3);setAnim(s,'idle')}return}
 if(u.state==='rest'){setAnim(s,'rest');u.timer-=dt;if(u.timer<=0||u.energy>82){u.state='idle';u.timer=rand(1,3);setAnim(s,'idle')}return}
 if(u.state==='graze'){setAnim(s,'eat');u.timer-=dt;if(Math.random()<dt*.6)burst(s.position,'dust',1);if(u.timer<=0)decide(s);return}
 setAnim(s,'idle');u.timer-=dt;if(u.timer<=0)decide(s)}

function updateUI(){grain.scale.y=.18+.82*(troughFood/100);grain.material.color.setHSL(.115,.55,.4+.14*(troughFood/100));water.material.opacity=.35+.65*(waterLevel/100);$('#feed').textContent=feedStock;$('#trough').textContent=Math.round(troughFood);$('#waterLevel').textContent=Math.round(waterLevel);if(selected){const u=selected.userData;$('#aname').textContent=u.name;$('#astate').textContent=(stateNames[u.state]||u.state)+' • '+u.personality;$('#hunger').style.width=(100-u.hunger)+'%';$('#thirst').style.width=(100-u.thirst)+'%';$('#energy').style.width=u.energy+'%';$('#social').style.width=u.social+'%'}}
function toast(t){const e=$('#toast');e.textContent=t;e.classList.add('on');clearTimeout(window.tt);window.tt=setTimeout(()=>e.classList.remove('on'),1800)}
$('#feedBtn').onclick=()=>{ensureAudio();if(feedStock<=0)return toast('خلص مخزون الشعير');const add=Math.min(35,100-troughFood);if(add<=0)return toast('المعلف ممتلئ');feedStock--;troughFood+=add;burst(feeder.position,'dust',8);toast('صبّيت الشعير في المعلف 🌾')};
$('#waterBtn').onclick=()=>{ensureAudio();waterLevel=100;toast('عبّيت حوض الماء 💧')};$('#truckBtn').onclick=()=>{ensureAudio();truckOn=!truckOn;toast(truckOn?'شغلت الوايت 🛻':'طفيت الوايت')};$('#marketBtn').onclick=()=>toast('السوق بنكمّله بعد ما نثبت حياة العزبة');$('#pet').onclick=()=>{if(!selected)return;selected.userData.social=clamp(selected.userData.social+18,0,100);burst(selected.position,'heart',6);bleat(.024);toast('تفاعلت مع '+selected.userData.name+' 🤚')};

const raycaster=new THREE.Raycaster(),pointer=new THREE.Vector2();let down=null;renderer.domElement.addEventListener('pointerdown',e=>{ensureAudio();down={x:e.clientX,y:e.clientY}});renderer.domElement.addEventListener('pointerup',e=>{if(!down||Math.hypot(e.clientX-down.x,e.clientY-down.y)>12)return;pointer.x=e.clientX/innerWidth*2-1;pointer.y=-(e.clientY/innerHeight)*2+1;raycaster.setFromCamera(pointer,camera);const hits=raycaster.intersectObjects(sheep,true);if(hits.length){let o=hits[0].object;while(o.parent&&!sheep.includes(o))o=o.parent;if(sheep.includes(o)){selected=o;$('#animal').classList.add('on');updateUI();bleat(.012)}}else{$('#animal').classList.remove('on');selected=null}});
let pointers=new Map(),last=null,lastD=0,target=new THREE.Vector3(),zoom=1;renderer.domElement.addEventListener('pointerdown',e=>{renderer.domElement.setPointerCapture(e.pointerId);pointers.set(e.pointerId,{x:e.clientX,y:e.clientY})});renderer.domElement.addEventListener('pointermove',e=>{if(!pointers.has(e.pointerId))return;pointers.set(e.pointerId,{x:e.clientX,y:e.clientY});const a=[...pointers.values()];if(a.length===1){if(last){target.x-=(a[0].x-last.x)*.017*zoom;target.z-=(a[0].y-last.y)*.017*zoom;target.x=clamp(target.x,-8,8);target.z=clamp(target.z,-8,8)}last={...a[0]}}else if(a.length>=2){const d=Math.hypot(a[0].x-a[1].x,a[0].y-a[1].y);if(lastD)zoom=clamp(zoom-(d-lastD)*.004,.58,1.5);lastD=d}});const end=e=>{pointers.delete(e.pointerId);last=null;lastD=0};renderer.domElement.addEventListener('pointerup',end);renderer.domElement.addEventListener('pointercancel',end);

loader.load(SHEEP_URL,g=>{sheepSource=g.scene;sheepAnims=g.animations||[];for(let i=0;i<10;i++)spawnSheep(i);done()},undefined,()=>$('#loading').textContent='تعذر تحميل موديل النجديات');loader.load(TRUCK_URL,g=>{truck=g.scene;shadows(truck);normalize(truck,2);truck.traverse(o=>{if(o.isMesh){o.material=o.material.clone();const n=(o.name||'').toLowerCase();if(n.includes('body'))o.material.color.set(0xeee9dd)}});truck.scale.multiplyScalar(1.65);truck.position.set(-6,0,8);truck.rotation.y=-.35;scene.add(truck);done()},undefined,()=>$('#loading').textContent='تعذر تحميل موديل الوايت');let loaded=0;function done(){loaded++;if(loaded>=2)setTimeout(()=>$('#loading')?.remove(),500)}

const clock=new THREE.Clock();let truckPhase=0,uiAcc=0,amb=0;function loop(){requestAnimationFrame(loop);const dt=Math.min(clock.getDelta(),.04),t=performance.now()/1000;for(const s of sheep){handleState(s,dt,t);proceduralPose(s,t)}mixers.forEach(m=>m.update(dt));if(truck&&truckOn){truckPhase+=dt*.22;const p=new THREE.Vector3(Math.sin(truckPhase)*7,0,7+Math.cos(truckPhase*.7)*1.8);const d=p.clone().sub(truck.position);if(d.length()>.2){d.normalize();truck.position.addScaledVector(d,1.45*dt);truck.rotation.y=THREE.MathUtils.lerp(truck.rotation.y,Math.atan2(d.x,d.z)-Math.PI/2,.06);if(Math.random()<dt*5)burst(truck.position,'dust',2)}}for(let i=fx.length-1;i>=0;i--){const q=fx[i];q.t-=dt;q.m.position.addScaledVector(q.v,dt);q.m.scale.multiplyScalar(1+dt*(q.type==='heart'?.45:1.6));q.m.material.opacity=clamp(q.t/(q.type==='heart'?1.1:.7),0,1);if(q.t<=0){scene.remove(q.m);fx.splice(i,1)}}amb+=dt;if(amb>8){amb=0;if(Math.random()<.5&&sheep.length)bleat(.012)}uiAcc+=dt;if(uiAcc>.14){uiAcc=0;updateUI()}const cp=new THREE.Vector3(target.x+13*zoom,18*zoom,target.z+22*zoom);camera.position.lerp(cp,.055);camera.lookAt(target);renderer.render(scene,camera)}loop();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});